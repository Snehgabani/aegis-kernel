[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "aegis-kernel"
version = "1.0.0"
description = "Aegis Invariant Kernel: Deterministic Tool-Call Safety Clearance Gateway for AI Agents"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "Aegis Project Contributors" }]
keywords = ["ai-safety", "agentic-ai", "langchain", "crewai", "autogen", "mcp", "security", "invariants"]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

[project.urls]
Homepage = "https://aegis-kernel.dev"
Repository = "https://github.com/aegis-kernel/aegis"
